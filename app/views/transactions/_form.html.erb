<%= form_for(@transaction) do |f|%>
  <%= f.label :account_type %>
  <%= f.select "account_type", options_for_select(["Checkings", "Savings"], "Checkings") %>

  <%= f.label :amount %>
  <%= f.number_field :amount %>

  <%= f.submit %>
<% end %>
